<%- include('../partials/header', { title: 'Editar Inscripción' }) %>
<%- include('admin_shell', { activeNav: 'inscripciones', topbarSection: 'Inscripciones' }) %>

  <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; margin-bottom: 1rem;">
    <div>
      <h1 style="color: var(--gold-light); font-size: 1.8rem;">✏️ Editar / Mover Inscripción</h1>
      <p style="color: var(--text-muted);">Modificá el torneo, la categoría, las notas o el estado de la inscripción.</p>
    </div>
  </div>

  <%- include('../partials/inscripcion_editar_form', {
    formAction: '/admin/inscripciones/' + reg.id + '/editar',
    baseUrl: '/admin/inscripciones/' + reg.id + '/editar',
    backUrl: '/admin/inscripciones',
    showStatus: true
  }) %>

</main>

<%- include('../partials/footer') %>
